{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/wxw-matt/devcontainer-features/command_runner:0": {
      "command1": "sudo apt-get update && sudo apt-get install -y build-essential flex bison libboost-filesystem-dev libboost-program-options-dev",
      "command2": "make grammar",
      "command3": "make compiler",
      "command4": "make simulator",
      "command5": "sudo make install"
    }
  }
}
